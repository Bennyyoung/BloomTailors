import React from 'react';
import SlideCarousel from './SlideCarousel';
import SideBar from './SideBar';

const Home = () => (
    <div>    
        <SlideCarousel />
        <SideBar />
    </div>
)

export default Home;